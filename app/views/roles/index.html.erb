

<div id="table_index">
<table>
  <thead>
    <tr>
      <th>Ime </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.name %></td>
        <td><%= link_to 'Ažuriraj', edit_role_path(role) %></td>
        <td><%= link_to 'Obriši', role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br><br>
<%= link_to '+ Nova rola', new_role_path %>

</div>
<br>

